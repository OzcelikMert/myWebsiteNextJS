import {NextRequest, NextResponse} from "next/server";

export default {
    checkBlackList: (
        req: NextRequest
    ) => {

    },
    checkCountry: (
        req: NextRequest
    ) => {

    }
};
